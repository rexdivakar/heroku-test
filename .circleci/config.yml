version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          name: Running files
          command: 
            python3 *.py
            python3 folder/*.py
      - store_artifacts:
          path: test-reports/
          destination: python_app